// Tooltip copy for controls the public demo disables.
export const DEMO_IMPORT_BLOCKED = 'Importing to the library is not available in the demo';

// For links out to pages that are themselves demo-restricted, so the dead end is visible
// before the click rather than after it.
export const DEMO_SECTION_BLOCKED = 'This section is not available in the demo';
